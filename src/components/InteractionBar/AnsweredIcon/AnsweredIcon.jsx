import React from "react";

import RoundedIcon from "../../RoundedIcon";

import styles from "./styles.scss";

const AnsweredIcon = () => {
  return <RoundedIcon className={styles.answered_icon} text="Answered" />;
};

export default AnsweredIcon;
